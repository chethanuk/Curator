# Paths Ray skips when this repo root is uploaded as a runtime_env working_dir
# (for example `ray job submit --working-dir .`). Leading "/" anchors to the repo root,
# so the nemo_curator/eval package is still uploaded.
/docs/
/eval/
/fern/
/tutorials/
